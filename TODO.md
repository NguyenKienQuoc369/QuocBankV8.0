# Task: Fix Login Page

- [ ] Rewrite `app/login/page.tsx` to be a proper login page
    - [ ] Import `login` action
    - [ ] Implement `StarField` and `CyberInput` components
    - [ ] Create login form with `username` and `password`
    - [ ] Handle success redirect to `/dashboard`
