// Legacy document removed - project uses App Router (`app/`).
// Keep a harmless default export to avoid build-time imports of `next/document`.
import React from 'react'

export default function DocumentPlaceholder() {
  return null
}
